<h1>Create an article</h1>

<!-- Dobrze bylo by to komencikami opisac :-) -->
<% if @article.errors.any? %>

<h2>The following errors prevented the article from getting created</h2>

<ul>
  <% @article.errors.full_messages.each do |msg| %>
  <li><%= msg %></li>
  <%end%>
</ul>
<%end%>
<!--calkiem sprytne prosty if sprawdzajacy czy sa jakies błedy jesli sa zostana one wyswietlone w formie listy, ciekawe jest to ze mozna zrobic literacje po błędach i dynamicznie utworzyć listę -->


<%=form_for @article do |f| %>
<p>
  <%= f.label :title %><br/>
  <%= f.text_field :title %>
</p>

<p>
  <%= f.label :description %><br/>
  <%= f.text_area :description %>
</p>

<p>
  <%= f.submit %>
  <!-- Skad Rails wie że ma teraz utworzyć artykuł ? ? ?
    Rails został tak napisany aby samemu się domyślać co ma zostać zrobione,
  zobacz sam ze nie nadaliśmy nazwy dla tego przycisku a na stronie wyświetla
się on jako 'Create article', wyglada na to ze metoda .subit po prostu słurzy do
  przeniesienia sie na akcję article create
end-->
</p>
<% end %>
