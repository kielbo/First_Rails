
<!-- table->tr-th -->
  <!-- Tutaj ciekawe zastosowanie, tworzymy table ustawiamy wiersze tytulowe, a potem puszczamy petle przez zmienna articles i uzupelniamy kolejne zedy tabelki jej wartosciami (zmiennej)dodatkowo dodane sa dwa pola z linkami do edycji i wyswietlenia wartości wpisu. -->
  <%@articles.each do |article|%>
    <div class="row">
      <div class="card col-md-8 offset-md-2">
        <div class ="article-title center">
          <%= link_to article.title, article_path(article)%>
        </div>
        <div class="article-description center">
          <%= truncate(article.description, length: 100) %>
        </div>
        <div class="article-action">
     <%=link_to 'Edit', edit_article_path(article),class:"btn btn-primary"%>
     <%=link_to 'Show', article_path(article),class:"btn btn-info"%>
     <button class="btn btn-danger"><%=link_to 'Delete', article_path(article),method: :delete,data:{confirm:"Are you sure ?"}%></button>
    </div>
      </div>
    </div>
    <%end%>

<button type="button" class="btn btn-secondary" data-toggle="tooltip" data-placement="top" title="Tooltip on top">
  Tooltip on top
</button>
